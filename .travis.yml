sudo: required
language: node_js
node_js:
  - "8"

services:
  - docker

cache: yarn
install: yarn

before_script:
  - sudo /etc/init.d/mysql stop

  script: 
  - yarn test
  - cd ./tests
  - docker-compose up
  - docker-compose ps
  - docker exec sql-mariadb ls


