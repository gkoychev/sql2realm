sudo: required
language: node_js
node_js:
  - "8"

services:
  - docker

cache: yarn
install: yarn

script: 
  - yarn test
  - cd ./tests
  - sudo netstat -alnp | grep 3306
  - ./setup_db.sh -d
  - docker-compose up
  - docker-compose ps
  - docker exec sql-mariadb ls


